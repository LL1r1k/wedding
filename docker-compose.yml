services:
  web:
    build: .
    ports:
      - "7080:80"
    environment:
      - ADMIN_PASS=${ADMIN_PASS}
    depends_on:
      - api
    restart: unless-stopped

  api:
    build: ./api
    environment:
      - ADMIN_USER=admin
      - ADMIN_PASS=${ADMIN_PASS}
      - DB_PATH=/data/rsvp.db
    volumes:
      - ./data:/data
    restart: unless-stopped
